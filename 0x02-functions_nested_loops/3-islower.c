#include "holberton.h"
#include<ctype.h>
/**
 *_islower -  checks a lowercase character
 *
 *@c: character to check
 *
 *Return: Success 1.
 */

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
islower(c);
return (1);
}
else
{
islower(c);
}
return (0);
}
