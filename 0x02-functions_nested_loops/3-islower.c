#include "holberton.h"
#include<ctype.h>
/**
 *_islower - checks a lowercase character
 *
 *@c: character to check
 *
 *Return 1: Success
 */

int _islower(int c)
{
if (c >= 'a'&& c <= 'z')
{
islower(c);
return (1);
}
else
{
islower(c);
}
return (0);
}
