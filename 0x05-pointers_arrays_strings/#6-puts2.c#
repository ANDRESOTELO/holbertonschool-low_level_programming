#include "holberton.h"

/**
 *puts2 - prints character of a string
 *@str: input string
 *
 */

void puts2(char *str)
{
int count;

for (count = 0; str[count] != '\0'; count++)
{
if ((str[count]%2) == 0)
_putchar(count);
}
putchar(10);
}
