#include<stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 *
 *Description: printf print formated. Write a format string
 */
int main(void)
{puts("with proper grammar, but the outcome is a piece of art,");
  return (0);
}
