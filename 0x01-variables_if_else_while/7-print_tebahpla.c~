#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 *
 */
int main(void)
{
int rv_alpha;
for (rv_alpha = 'z'; rv_alpha >= 'a'; rv_alpha++)
putchar(rv_alpha);
putchar('\n');
return (0);
}
