#include "holberton.h"
/**
 *_isdigit - checks a digit character
 *@c: character to check
 *Return: Success 1
 *
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
}
return (0);
}
